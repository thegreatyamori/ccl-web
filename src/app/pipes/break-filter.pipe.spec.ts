import { BreakFilterPipe } from './break-filter.pipe';

describe('BreakFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BreakFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
