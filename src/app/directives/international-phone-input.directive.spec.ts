import { InternationalPhoneInputDirective } from './international-phone-input.directive';

describe('InternationalPhoneInputDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new InternationalPhoneInputDirective();
    expect(directive).toBeTruthy();
  });
});
