export interface BaseColors {
  colors: Colors;
  classes: Colors;
}

export interface Colors {
  jovenes: string;
  damas: string;
  caballeros: string;
  matrimonios: string;
  title: string;
  active: string;
}
